class Solution {
public:
    int findGCD(vector<int>& nums) {
        sort(nums.begin(),nums.end());
        
        int Smallest_Number=nums[0];

        int Largest_Number=nums[nums.size()-1];

       return gcd(Smallest_Number,Largest_Number);
    }
};
